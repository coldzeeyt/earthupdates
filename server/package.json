{
  "name": "earthupdates-admin-api",
  "version": "1.0.0",
  "private": true,
  "description": "Passcode-gated backend that publishes patch notes to data/patches.json in the earthupdates repo, holding the GitHub write token server-side.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  }
}
